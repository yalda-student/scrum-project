در کلاس Constants برای تایپ فاکتور ها بک سری ثابت نوشته شده. در صورت نیاز از آن استفاده کنید.

در لایه طراحی مربوط به ساخت فاکتور یک spinner قرار داده شده تا با نام اعضای گروه جای پر شود. ایده من برای پیاده سازی قسمت مقدار دهی
هر عضو به این صورت بود که با spinner و ادیت تکست برای مقدار سهم باشد. اگر مشکلی داشت یا ایده کاربر پسند تری بود، اطلاع بدید
تا تغییرات اعمال شه؛ ممنونم :)

در دایرکتوری api نیز مدل ها پیاده سازی شده اند.

  طراحی لایه طراحی  لیست فاکتور هاایجاد شده است. و در کلاس InvoiceAdapter به اداپتر آن bind شده اما کلاس پیاده سازی نشده.

 در GroupActivity هنگامی که در منوی بالای صفحه آپشن فاکتور انتخاب می شود اپلیکیشن وارد InvoiceActivity می شود.(انجام شده این بخش)
 در InvoiceActivity  لیست خرج ها یا همان فاکتور ها را مشاهده می کند.
 با کلیک روی FAB می تواند خرج جدید ایجاد کند و وارد CreateInvoiceActivity می شود.
  وقتی روی هر ایتم خرج یا همان فاکتور کلیک می شود وارد ViewInvoiceActivity می شود.

  در ViewInvoiceActivity با استفاده از table layout اطلاعات آن فاکتور نمایش داده میشود.
  رنگ تیتر جدول آبی و مقادیر مشکی می باشد. تیتر ها bold شده اند اما مقادیر نه.